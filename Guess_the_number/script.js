let person = window.prompt("Welcome! What is your name?");
if (person == "") {
  alert("Please enter you name");
} else {
  alert("Hey " + person);
}

var min = 0; 
var max = 25;
var number = 15;
    
const guessNumber = function (min, max) {
    const play = prompt("Enter a number between 0 and 25 to start guessing...");
    if (guessnumber != number){
        alert('Unfortunalety, that is not correct! Please try again!');
        return Math.random(15) * (max - min) + min;
    }else{ (guessNumberJuan == number) 
        alert('Congratulations, you have won the game!');
        return true;
    }
  } 

  // Ik heb deze opdracht echt niet kunnen oplossen. 